package com.hujian.concurrent;

import java.util.concurrent.ScheduledThreadPoolExecutor;

public class ScheduledThreadPoolExecutorRun {
public static void main(String[] args) {
	ScheduledThreadPoolExecutor p = new ScheduledThreadPoolExecutor(2);
	//p.schedule(command, delay, unit);
	//p.scheduleAtFixedRate(command, initialDelay, period, unit);
	//p.scheduleWithFixedDelay(command, initialDelay, delay, unit)
}
}
